(function () {
    'use strict';
    var userService = angular.module('userService', ['ngResource']);

    userService.factory('User', ['$http',
    function ($http) {
        return {

        }
    }]);


})();